/**
 * creating variables
 * dynamically typed language
 * cannot be answered for JIT compilations or interpretation or compiler
 * transpiled
 */

let person = null // recommended
var age = undefined // not recommended
const birthdate = 1990 // for fixing the reference/values

// birthdate = 2000 // modification is not allowed
console.log(birthdate);

console.log(person) // null
console.log(age) //undefined
